export interface NomPracticeDto {
    id: number,
    name: string,
}

export interface NomAreaDto {
    id: number,
    name: string,
}

export interface StatusDto {
    id: number,
    name: string,
    description: string
}